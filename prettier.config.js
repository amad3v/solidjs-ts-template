export default {
  semi: true, // Use semicolons at the end of statements
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'all', // Add trailing commas wherever possible (e.g., in objects, arrays)
  printWidth: 100, // Line length limit before wrapping
  tabWidth: 2, // Number of spaces per indentation level
  useTabs: false, // Indent with spaces, not tabs
  bracketSpacing: true, // Print spaces between brackets in object literals
  arrowParens: 'always', // Always include parentheses around arrow function arguments
  endOfLine: 'lf', // Use 'lf' (Line Feed) for line endings (good for cross-platform consistency)
};
